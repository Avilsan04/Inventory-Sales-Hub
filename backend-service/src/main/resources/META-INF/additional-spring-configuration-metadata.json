{
  "properties": [
    {
      "name": "app.jwt.secret",
      "type": "java.lang.String",
      "description": "Secret key for signing JWT tokens (min. 32 chars, injected via JWT_SECRET env var)."
    }
  ]
}

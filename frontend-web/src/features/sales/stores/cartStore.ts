// Cart store moved to entities/cart — re-exported here for backwards compat within this feature.
export { useCartStore } from '@entities/cart';
export type { CartState, CartItem } from '@entities/cart';

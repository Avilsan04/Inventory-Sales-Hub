// CartProvider has been removed. The catalog cart now delegates to the unified
// Zustand store (useCartStore) via the useCart adapter in useCart.ts.
// This file is kept as a no-op to avoid breaking any stale tooling caches.
export {};
